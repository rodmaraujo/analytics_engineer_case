

# Using DBT + WSL2 (22 is my version) + postgresql docker desktop container


# Navigate to your prefered folder (my example):
cd /mnt/e/case_analytics_engineer

# create a project
mkdir dbt_project

cd dbt_project  # !!!!!!!!


# Python isolated enviroment
sudo apt update
sudo apt install python3-venv

python3 -m venv venv

source venv/bin/activate 
                            or deactivate


# Pip + dbt-core + dbt-postgres
pip install --upgrade pip setuptools wheel

pip install dbt-postgres    

#DBT init configs:

dbt init .

# Name your project: 	    "name" + <enter>
# Profile name	            "dbt_postgres"
# Which database adapter?	"postgres"
#Host	                    "localhost"         #in my case
#Port	                    5454                #in my case
#Database	                "dbt_database"      #in my case
#Username	                "dbt_user"          #in my case
#Password	                "dbt_password"      #in my case 
#Schema	                    "loadsmart"         #my default schema


# packages.yml
packages:
  - package: dbt-labs/dbt_utils
    version: 1.3.0

dbt deps

